<div class="row mt-5">
    <div class="col-md-8  mx-auto">
         <div class="card mt-10">
             <div class="card-body">
                 <div class="">
                      <table id="employeeTable" class="display" style="width:100%">
                           <thead>
                              <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Phone Number</th>
                                <th>Department</th>
                                <th>Hourly Pay</th>
                                <th></th>
                            </tr>
                            </thead>
                            <!--this is where we need to get the information from each user-->
                             <tbody>
                                 {{#each users}}
                              <tr>
                                 <td>{{firstName}} {{lastName}}</td>
                                 <td>{{email}}</td>
                                 <td>{{phoneNumber}}</td>
                                 <td>{{department}}</td>
                                  <td>${{hourlyPay}}</td>
                                  <!--add link to employee id so when its clicked -->
                                  <td><a href="#" class="btn btn-info btn-lg">
                                        <span class="glyphicon glyphicon-pencil"></span> Edit 
                                      </a>
                                  </td>
                             </tr>
                             {{/each}}
                          </tbody>
                  </table>
                </div>
            </div>
        </div>
    </div>
</div>